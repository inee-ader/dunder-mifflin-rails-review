<h1><%= @dog.name %></h1>
    <h3>Breed: <%= @dog.breed%></h3>
    <h3>Age: <%= @dog.age%></h3>
    <h3>Owners: </h3>
        <ul>
        <% @dog.employees.each do |emp| %>
            <li><%= link_to emp.to_s, employee_path(emp) %></li>
            <% end %>
        </ul>
    </h3>
